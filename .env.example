# GoDrive Bislig backend (Supabase). Copy to .env.local and fill in.
# Never commit real keys. Never use a service-role secret in browser code.
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_paste_here"
